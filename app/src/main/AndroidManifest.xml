<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <!-- BLE 扫描发现手机（Android 9 需要定位运行时权限，ADB 授予） -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- 本地 HTTP 分发手机端 APK（无公网，仅车机热点/内网） -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.WRITE_SETTINGS" />
    <!-- 壁纸/地图场景检测（ADB appops 允许 GET_USAGE_STATS） -->
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />

    <uses-feature
        android:name="android.hardware.bluetooth_le"
        android:required="false" />

    <application
        android:name=".IcarApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="IcarLyrics"
        android:theme="@android:style/Theme.Material.NoActionBar">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 悬浮窗渲染服务：WebView 加载 lyrics_overlay.html -->
        <service
            android:name=".OverlayService"
            android:exported="false" />

        <!-- 车机蓝牙媒体会话监听（歌词时间轴可信源，需 ADB 授权） -->
        <service
            android:name=".CarMediaListener"
            android:exported="false"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- BLE GATT 服务端：接收手机推送的歌词/进度/指令 -->
        <service
            android:name=".BleService"
            android:exported="false" />

        <!-- 开机自启（悬浮窗权限已授予时生效） -->
        <receiver
            android:name=".BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!-- ADB 直控（v2.0）：am broadcast -a com.icarme.lyrics.START/STOP/SET_PHONE -->
        <receiver
            android:name=".AdbReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.icarme.lyrics.START" />
                <action android:name="com.icarme.lyrics.STOP" />
                <action android:name="com.icarme.lyrics.SET_PHONE" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
