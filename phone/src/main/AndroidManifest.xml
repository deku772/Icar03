<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <!-- BLE 扫描在 targetSdk<=30 需要，运行时申请 -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <uses-feature
        android:name="android.hardware.bluetooth_le"
        android:required="true" />

    <application
        android:name=".IcarPhoneApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="IcarLyrics 手机端"
        android:theme="@android:style/Theme.Material.Light.NoActionBar">

        <activity
            android:name=".PhoneMainActivity"
            android:exported="true"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 总调度前台服务：播放监控 + 取词 + BLE 推送 -->
        <service
            android:name=".PlaybackService"
            android:exported="false" />

        <!-- 通知使用权（授权后读取媒体会话与播放进度） -->
        <service
            android:name=".NotificationListener"
            android:exported="false"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- ADB 直控（v1.8.2）：am broadcast -a com.icarme.lyrics.phone.START/STOP -->
        <receiver
            android:name=".AdbReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.icarme.lyrics.phone.START" />
                <action android:name="com.icarme.lyrics.phone.STOP" />
            </intent-filter>
        </receiver>

        <!-- 开机/覆盖安装自启（手动停止后不再拉起） -->
        <receiver
            android:name=".BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
