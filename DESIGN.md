# iCAR 车机设置页 · 模式 A 设计说明

> 依据《iCAR 车机 UI 设计规范》落地 `IcarLyrics` 车机设置页。
> 本文件是施工蓝图；数值来源标注见规范正文。

## 视觉方向

- **风格锚点**：奇瑞 iCAR 原车「设置 → 显示」双栏浮窗（`com.chery.carsettings`），不是 Material 调试台、不是深色仪表盘。
- **背景**：左侧半透明单色 + 右侧纵向三点渐变；根窗口透明，避免二次合成。
- **强调**：默认紫 `#5C66BF`；只读 `com.mb.provider.theme_key`，粉 `33/16 → #DE5185`，紫 `32 → #5C66BF`。强调只用于导航选中、分段选中、开关开启与必要焦点。
- **文案层级**：Body1 `31.75sp` / Body2 `27.25sp` / Caption2 `22.75sp`；字号表层级，字重表选中，颜色表状态，不叠出第三套。
- **控件**：分段轨高 `95dp`（外圆角 `20` / 选中 `14`）；开关按原车 `64:36` 坐标空间绘制，触控约 `73×41dp`；设置卡约 `466×143dp`。
- **窗口**：目标车机标准右侧浮窗 `1230×810px` @(660,90)；进 `fromYDelta=100%p→0 /150ms`，出 `0→100%p /300ms`，绑定在窗口动画。

## 布局系统

```
┌──────────┬────────────────────────────┐
│ 左导航    │ 右内容                      │
│ 345dp    │ pad L48 R41 T/B48          │
│ 标题+角标 │ 组标题 → 控件 → 组间距 40   │
│ 95dp 行  │ 分段 / 开关卡 / 整行动作卡  │
│ 底「关于」│ 统一纵向滚动                 │
└──────────┴────────────────────────────┘
```

- 左栏 `345dp`，内边距 `29.5dp`，菜单项 `286×95dp`。
- 右栏 padding：左 `48` / 右 `41` / 上下 `48`。
- 组标题到控件 `20dp`，组间距 `40dp`。
- 信息弹窗：全窗遮罩 + `860dp` 内容面 + `48dp` 内边距 + `95dp` 主按钮；不用系统 Toast/Dialog 承载设置结果。

## 信息架构（本仓库可完成的任务）

规范完整分类为六类；本产品当前真实能力映射如下，**不展示无法操作的空分类**：

| 分类 | 内容 | 说明 |
|---|---|---|
| 歌词设置 | 开机自启动、悬浮调试、歌词颜色（白/黑/跟随系统）、歌词对齐、歌词偏移 | 模式 A 板块结构；颜色默认「跟随系统」 |
| 服务状态 | 悬浮服务、BLE、连接、手机地址；扫描手机；启停服务；下载 APK | 状态用信息卡；一次性命令用整行动作卡 |
| 关于 | 版本号、项目地址、ADB 授权帮助、下载/赞赏二维码 | 底部固定入口 |

缓存 / 查找 / 权益中心：规范预留，本仓库功能未落地前不占导航，避免假入口。

## 歌词悬浮层边界

- 不套用设置双栏、不套用应用网格。
- 边缘渐隐：可视容器上下各 `64px` CSS alpha mask，中部原样；不用 backdrop-filter / 帧循环。
- 「跟随系统」时焦点色注入为当前设置强调色；白/黑仍用原有可读对比色板。
- 标准浮窗生命周期动效只绑设置 Activity，不绑悬浮歌词。

## 证据等级

| 项 | 等级 | 来源 |
|---|---|---|
| 几何/色板/圆角 token | A→D | 规范原车资源表 + 项目 dp 换算 |
| 浮窗边界与动效 | A/B/D | 规范 §3.1，本项目采用 |
| theme_key 32/33 | B/C | 规范 §5.4 已实机闭环 |
| 其余主题键 | — | 未确认，不做纯规则全量宣称 |
| 分类裁剪 | D | 本项目功能边界 |
